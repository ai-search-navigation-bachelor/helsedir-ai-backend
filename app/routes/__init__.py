"""
Routes package — HTTP endpoint definitions (thin layer).

Each module maps URL paths to controller calls. No business logic lives here;
routes only parse requests and delegate to the relevant controller.
"""
